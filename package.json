{
  "name": "eleventy-generate-category-pages",
  "version": "0.0.7",
  "description": "Generates paginated category pages as part of an eleventy site build.",
  "author": "John M. Wargo",
  "license": "MIT",
  "main": "eleventy-generate-category-pages.js",
  "scripts": {
    "test": "tsc && echo \"Launching test\" && node ./test/test.js && echo \"Test complete\"",
    "build": "tsc"
  },
  "keywords": [
    "Eleventy"
  ],
  "devDependencies": {
    "@types/fs-extra": "^11.0.1",
    "@types/node": "^20.2.5",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "cli-logger": "^0.5.40",
    "fs-extra": "^11.1.1",
    "yaml": "^2.3.1"
  }
}
